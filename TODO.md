TODO instructions:

- Read all items and ask for clarification before implementing.
- If a feature is already implemented, let the user know before re-implementing.

TODO list:

- Create an AGENTS.md file.
- Fix a plotting error (see image in ./TODO/plot_error.png).
- Allow a gauge to be edited.
- Allow adding a DBC file.
- In "Discover" tab, show the DBC equivalent, when it exists.
- Create a gauge specific for temperatures (can look like a thermometer).
- Create a gauge specific for pressure (can look like a manometer).
- Allow unit conversion (even if the DBC has the data coming as km/h, allow changing to mph, for example).
- Create a gauge which will have a formula as its input, so we can have things like "Average Km/l".
- When a replay is being played, allow play/pause and show a timeline the user can drag.

